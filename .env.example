# SugarCRM ↔ PandaDoc Middleware Environment Configuration

# Database Configuration (Required)
DATABASE_URL=postgresql://user:password@host:port/database

# Authentication Configuration (Choose one mode)

# Option 1: Token Authentication (Recommended for Production)
AUTH_MODE=token
ADMIN_TOKEN=your-secure-production-token-2025

# Option 2: Basic Authentication (Web-friendly)
# AUTH_MODE=basic
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your-secure-password

# Option 3: No Authentication (Development only)
# AUTH_MODE=none
# WARNING: Only use for development or internal networks

# Option 4: OAuth Integration (Future)
# AUTH_MODE=oauth
# OAUTH_PROVIDER=your-oauth-provider

# Optional Configuration
NODE_ENV=production
PORT=5000

# Cloud Provider Specific Examples:

# AWS Lambda
# DATABASE_URL=postgresql://user:pass@rds-instance.region.rds.amazonaws.com:5432/middleware

# Google Cloud SQL
# DATABASE_URL=postgresql://user:pass@/dbname?host=/cloudsql/project:region:instance

# Azure Database for PostgreSQL
# DATABASE_URL=postgresql://user:pass@server.postgres.database.azure.com:5432/middleware?sslmode=require

# Heroku Postgres (automatically provided)
# DATABASE_URL is automatically set by Heroku

# Development
# DATABASE_URL=postgresql://localhost:5432/middleware_dev
# AUTH_MODE=none